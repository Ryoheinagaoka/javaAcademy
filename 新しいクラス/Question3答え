int num は基本データ型
int[] arrayは参照型

twiceメソッドのnumは引数で、
メインメソッドの中で定義されているnumの値を変化を加えるものではない。
処理としては、メソッドの引数int num にメインメソッドのnumの値10が渡され、
計算後メソッドの引数のint numの値が20に変化する処理となる。

しかし、arrayは配列で参照型の為、
計算後変化するのはarrayの参照先の値。

その為、基本データ型のnumは変化せず、
参照型のarrayは変化した。